import React from "react";
import LoginForm from "../components/LoginForm";

const UserProfile = () => {
  return (
    <section>
      <LoginForm />
    </section>
  );
};

export default UserProfile;
